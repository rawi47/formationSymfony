{% extends 'base.html.twig' %}

{% block title %}Doccument{% endblock %}

{% block body %}
    <h1>Doccument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ doccument.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('doccument_index') }}">back to list</a>

    <a href="{{ path('doccument_edit', {'id': doccument.id}) }}">edit</a>

    {{ include('doccument/_delete_form.html.twig') }}
{% endblock %}
